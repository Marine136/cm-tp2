<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ipvc.estg.cm_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CMapp">
        <activity android:name=".MarcarConsulta"></activity>
        <activity android:name=".Questionarios" />
        <activity android:name=".CalendarioPaciente" />
        <activity android:name=".ScreenPaciente" />
        <activity android:name=".InformacaoPacientes" />
        <activity android:name=".DefinicoesUsers" />
        <activity android:name=".CalendarioPsicologo" />
        <activity android:name=".ScreenPsicologo" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>